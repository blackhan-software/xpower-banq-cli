name: Release Tag

on:
  push:
    tags:
      - "v*" # triggers workflow on pushing a tag

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        platform: [linux, macos, mswin]
        arch: [x64]
        exclude:
          - os: ubuntu-latest
            platform: macos
          - os: ubuntu-latest
            platform: mswin
          - os: macos-latest
            platform: linux
          - os: macos-latest
            platform: mswin
          - os: windows-latest
            platform: linux
          - os: windows-latest
            platform: macos

    steps:
      - name: Setup repo
        uses: actions/checkout@v4
      - name: Setup deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Setup env
        run: cp .env-avalanche-main .env

      - name: Run install
        run: deno install --node-modules-dir=auto --allow-scripts=npm:usb@2.9.0,npm:node-hid@2.1.2,npm:keccak@3.0.4
      - name: Run linter
        run: deno lint
      - name: Run tests
        run: deno test -A

      - name: Build artifact(s)
        run: deno task build-banq-${{ matrix.platform }}
      - name: List artifact(s)
        run: ls -R ./dist
      - name: Upload artifact(s)
        uses: actions/upload-artifact@v4
        with:
          name: banq-${{ matrix.platform }}
          path: ./dist/banq.*

  release:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Download artifact(s)
        uses: actions/download-artifact@v4
        with:
          path: ./dist
      - name: List artifact(s)
        run: ls -R ./dist

      - name: Install GitHub CLI
        run: sudo apt-get install -y gh
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create ${{ github.ref_name }} \
            -t "XPower Banq CLI ${{ github.ref_name }}" \
            -F RELEASE.md ./dist/banq-*/banq.*
